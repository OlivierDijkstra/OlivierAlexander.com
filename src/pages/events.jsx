import { Button } from '@iseats/apollo';
import Layout from 'components/Layout/Layout';

function Page() {
    return (
        <Layout>
            <Button>Hello</Button>
        </Layout>
    );
}

export default Page;
